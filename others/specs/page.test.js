describe("QAcart Android E2E", () => {
  it("[1] selection by ", async () => {
    //cy.get('#id)

    //accessibility id
    // await $("~");
    // await driver.pause(1000);

    //xPath
    // 
    
    //UIAutomator
    // (await $('android=new UiSelector().className("xxx").index(xxx)')).click();
  });
});